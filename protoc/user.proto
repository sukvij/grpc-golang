syntax = "proto3";

option go_package = "./";

message User {
    int32 id = 1;
    string name = 2;
}

message userIdInput {
    string userId = 1;
}

message Empty {}

service Example {
    rpc GellUserById (userIdInput) returns (User);
    rpc GetAllUser (Empty) returns (stream User);
}